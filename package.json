{
  "name": "linkcheck-action",
  "version": "1.0.0",
  "description": "GitHub Action for checking broken links using muffet",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt",
    "test": "node --test src/*.test.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mithro/linkcheck-action.git"
  },
  "keywords": [
    "github-action",
    "link-checker",
    "muffet",
    "broken-links"
  ],
  "author": "mithro",
  "license": "Apache-2.0",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/tool-cache": "^2.0.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
